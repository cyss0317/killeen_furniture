<div class="max-w-2xl mx-auto py-12 px-4">
  <h1 class="text-2xl font-bold text-warm-950 mb-8">Account Settings</h1>

  <div class="bg-warm-50 rounded-2xl border border-warm-400 p-8 mb-6">
    <h2 class="text-lg font-semibold text-warm-900 mb-6">Personal Information</h2>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="space-y-5">
        <div class="grid grid-cols-2 gap-4">
          <div>
            <%= f.label :first_name, class: "block text-sm font-medium text-warm-800 mb-1" %>
            <%= f.text_field :first_name, autocomplete: "given-name",
                class: "w-full px-4 py-2.5 border border-warm-400 rounded-xl bg-white text-warm-900 focus:outline-none focus:ring-2 focus:ring-warm-400 focus:border-transparent" %>
          </div>
          <div>
            <%= f.label :last_name, class: "block text-sm font-medium text-warm-800 mb-1" %>
            <%= f.text_field :last_name, autocomplete: "family-name",
                class: "w-full px-4 py-2.5 border border-warm-400 rounded-xl bg-white text-warm-900 focus:outline-none focus:ring-2 focus:ring-warm-400 focus:border-transparent" %>
          </div>
        </div>

        <div>
          <%= f.label :email, class: "block text-sm font-medium text-warm-800 mb-1" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email",
              class: "w-full px-4 py-2.5 border border-warm-400 rounded-xl bg-white text-warm-900 focus:outline-none focus:ring-2 focus:ring-warm-400 focus:border-transparent" %>
          <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
            <p class="mt-1 text-sm text-warm-600">Awaiting confirmation for: <%= resource.unconfirmed_email %></p>
          <% end %>
        </div>

        <hr class="border-warm-200" />

        <div>
          <h3 class="text-base font-semibold text-warm-900 mb-1">Change Password</h3>
          <p class="text-sm text-warm-600 mb-4">Leave blank to keep your current password.</p>
          <div class="space-y-4">
            <div>
              <div class="flex items-center justify-between mb-1">
                <%= f.label :password, "New password", class: "block text-sm font-medium text-warm-800" %>
                <% if @minimum_password_length %>
                  <span class="text-xs text-warm-600"><%= @minimum_password_length %> characters minimum</span>
                <% end %>
              </div>
              <%= f.password_field :password, autocomplete: "new-password",
                  class: "w-full px-4 py-2.5 border border-warm-400 rounded-xl bg-white text-warm-900 focus:outline-none focus:ring-2 focus:ring-warm-400 focus:border-transparent" %>
            </div>
            <div>
              <%= f.label :password_confirmation, "Confirm new password", class: "block text-sm font-medium text-warm-800 mb-1" %>
              <%= f.password_field :password_confirmation, autocomplete: "new-password",
                  class: "w-full px-4 py-2.5 border border-warm-400 rounded-xl bg-white text-warm-900 focus:outline-none focus:ring-2 focus:ring-warm-400 focus:border-transparent" %>
            </div>
          </div>
        </div>

        <hr class="border-warm-200" />

        <div>
          <%= f.label :current_password, "Current password", class: "block text-sm font-medium text-warm-800 mb-1" %>
          <p class="text-xs text-warm-600 mb-1">Required to save any changes above</p>
          <%= f.password_field :current_password, autocomplete: "current-password",
              class: "w-full px-4 py-2.5 border border-warm-400 rounded-xl bg-white text-warm-900 focus:outline-none focus:ring-2 focus:ring-warm-400 focus:border-transparent" %>
        </div>

        <div class="flex gap-3 pt-2">
          <%= f.submit "Save changes", class: "bg-warm-800 hover:bg-warm-700 text-warm-50 font-semibold py-2.5 px-6 rounded-full transition-colors cursor-pointer" %>
          <%= link_to "Cancel", account_profile_path, class: "bg-white border border-warm-400 text-warm-700 font-semibold py-2.5 px-6 rounded-full hover:bg-warm-100 transition-colors" %>
        </div>
      </div>
    <% end %>
  </div>

  <div class="bg-warm-50 rounded-2xl border border-red-200 p-8">
    <h2 class="text-lg font-semibold text-red-800 mb-2">Delete Account</h2>
    <p class="text-sm text-warm-700 mb-4">Permanently delete your account and all associated data. This cannot be undone.</p>
    <%= button_to "Delete my account", registration_path(resource_name),
        data: { turbo_confirm: "Are you sure you want to delete your account? This cannot be undone." },
        method: :delete,
        class: "bg-red-600 hover:bg-red-700 text-white font-semibold py-2.5 px-6 rounded-full transition-colors cursor-pointer" %>
  </div>
</div>
