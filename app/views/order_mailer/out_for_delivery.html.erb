<div class="container">
  <div class="header">
    <p class="header-wordmark">Warehouse Furniture</p>
    <p class="header-subtitle">Your order is on its way!</p>
  </div>

  <div class="body">
    <p>Hi <%= @order.customer_name %>,</p>
    <p>Great news! Your order is out for delivery today and should arrive soon.</p>

    <div class="info-box">
      <p><strong>Order Number:</strong> <%= @order.order_number %></p>
      <p><strong>Status:</strong> Out for Delivery</p>
    </div>

    <div class="section">
      <p class="section-title">Delivery Address</p>
      <p style="font-size: 14px; line-height: 1.7; margin: 0;">
        <%= @order.shipping_address["full_name"] %><br>
        <%= @order.shipping_address["street_address"] %><br>
        <%= @order.shipping_address["city"] %>, <%= @order.shipping_address["state"] %> <%= @order.shipping_address["zip_code"] %>
      </p>
    </div>

    <div class="section">
      <p class="section-title">Items Being Delivered</p>
      <table class="items">
        <% @order_items.each do |item| %>
          <tr>
            <td>
              <% if item.product %>
                <a href="<%= product_url(item.product) %>" style="font-weight: 600;"><%= item.product_name %></a>
              <% else %>
                <strong><%= item.product_name %></strong>
              <% end %>
              <span class="item-meta">SKU: <%= item.product_sku %></span>
            </td>
            <td style="text-align: right; white-space: nowrap;">&times; <%= item.quantity %></td>
          </tr>
        <% end %>
      </table>
    </div>

    <p class="muted" style="margin-top: 24px;">Please ensure someone is available to receive the delivery. If you have any questions, feel free to contact us.</p>
  </div>

  <div class="footer">
    <p>Warehouse Furniture · 1104 E Veterans Memorial Blvd, Killeen, TX 76541</p>
    <p>© <%= Time.current.year %> Warehouse Furniture. All rights reserved.</p>
  </div>
</div>
