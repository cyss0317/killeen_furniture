<% content_for :title, "Edit Announcement — Admin" %>

<div class="flex items-center gap-4 mb-6">
  <%= link_to "← Announcements", admin_announcements_path, class: "text-sm text-gray-500 hover:text-gray-700" %>
  <h1 class="text-2xl font-bold text-gray-900">Edit Announcement</h1>
</div>

<div class="max-w-2xl">
  <%= render "form", announcement: @announcement %>
</div>
