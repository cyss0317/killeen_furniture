<% content_for :title, "Edit Zone" %>
<div class="flex items-center gap-4 mb-6">
  <%= link_to "← Delivery Zones", admin_delivery_zones_path, class: "text-sm text-gray-500 hover:text-gray-700" %>
  <h1 class="text-2xl font-bold text-gray-900">Edit: <%= @zone.name %></h1>
</div>
<div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
  <%= render "admin/delivery_zones/form", zone: @zone %>
</div>
